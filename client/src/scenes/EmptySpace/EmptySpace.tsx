import "../../sass/layout/_emptySpace.scss";

const EmptySpace = () => {
  return (
    <div className="empty__space">
      Unlock the secrets within a folder to unveil a world of possibilities for
      your files
    </div>
  );
};

export default EmptySpace;
